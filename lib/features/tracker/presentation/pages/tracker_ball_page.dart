import 'package:flutter/material.dart';

import '../tracker_ball.dart';

class TrackerBallPage extends StatelessWidget {
  const TrackerBallPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
      body: TrackerBall(),
    );
}
